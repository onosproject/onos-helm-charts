apiVersion: v1
kind: Service
metadata:
  name: {{ include "onos-ran.fullname" . }}
  labels:
    app: {{ template "onos-ran.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    {{- include "onos-ran.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    name: {{ template "onos-ran.fullname" . }}
    app: onos
    type: ran
    resource: {{ template "onos-ran.fullname" . }}
    {{- include "onos-ran.selectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: 5150
